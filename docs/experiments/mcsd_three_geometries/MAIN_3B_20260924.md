# First single-seed 3B batch, 2026-09-24

Status: six jobs submitted. At approximately 15:28 HKT, the three Frobenius
runs had reached step 10/11445 without NaN/skipped updates. The three Stiefel
runs were waiting on their respective predecessors. This is the first full-
budget practical comparison, not a completed, tuned, multi-seed paper result.

## Gates and scope

All ten [100M pilots](PILOT_20260924.md) completed normally. Advance the full
three-method group for Frobenius and the full three-method group for Stiefel,
including MuonH and iMuon, which beat MCSD/TP at the pilot endpoint.
The logged return defects in these groups were below 3e-7; this is sparse
evidence, not a per-update exact-feasibility certificate.

Defer all four spectral methods. Their largest persisted post-update norm
defects were 5.28% (MuonSphere), 13.76% (SSO), 5.23% (MCSD), and 15.76%
(MCSD-TP). Baselines use pre-return/update order, unlike post-return MCSD/TP.
The new cold-PI code must not be conflated with the old cached warm PI control.
No formal spectral jobs were submitted in this batch.

No optimizer/model kernel changed between pilot and main. Source commit
`900c4e347f32419b7a0c07a7d829723c84631e6e` adds only launcher support and tests
for Megatron's existing checkpoint-retention option. All 74 local focused
tests passed. H20 gate 4106552 completed four real-model steps in 00:01:48,
saved step 4 successfully, retained `latest_checkpointed_iteration.txt = 4`,
and removed step 2 only after the new save succeeded.

## Shared settings and limits

- Model/initialization/logical layout, data prefixes, tokenizer, numerical
  kernels, auxiliary AdamW and method-specific momentum match the pilot.
- One H20 per independent run, TP=PP=DP=1; three concurrent jobs maximum.
- Width 384, 126,641,024 parameters, LR 0.01, seed 1234, global batch 128,
  micro batch 4, sequence 2048.
- Fresh initialization, no pilot checkpoint continuation. Main horizon is
  11445 steps / 3,000,238,080 processed tokens, with 229 warmup steps and
  cosine decay to LR 0.001. The horizon changes from the pilot by design.
- Validation: 40 batches every 200 steps and at the final endpoint. Same
  data order/budget/schedule within this batch, not a promise of identical
  pilot-versus-main sample indices at a changed Megatron horizon.
- Save every 2000 steps; `--save-retain-interval 12000` means only the latest
  successful checkpoint remains within the 11445-step run. Retain the final
  checkpoint for later independent evaluation. Older intermediate checkpoints
  in each new run are intentionally replaced; old-project results are untouched.
- Measured checkpoint size: 1,575,530,498 bytes. Six retained plus three
  simultaneous replacement writes and a 3-GiB reserve require approximately
  17.40 GB; submission-time available space was 20.83 GB. This is an estimate,
  not a guarantee against other jobs writing to the same filesystem.
- Per-job time limit: 48 hours. Pilot throughput suggests approximately
  25--28 hours per run including scheduled evaluation, or about 2--2.5 days
  for two dependency waves if the resources remain available. This is an
  extrapolation, not a measured 3B runtime or guaranteed queue-start time.

MCSD/TP and iMuon use beta 0.95 without Nesterov; MuonH uses beta 0.9 with
Nesterov, as in the pilot. Comparisons do not isolate the tangent projection
from momentum differences. No equal-budget LR sweep or paired-seed repeat has
yet established the best hyperparameters for this model/budget.

## Job register

Run paths below are relative to
`/home/u3013198/projects/SSO_test-three-geometries/results/manifold/`.

| Job | Constraint | Method | Successful pilot | Predecessor | Run directory |
| ---: | --- | --- | ---: | ---: | --- |
| 4106582 | Frobenius | MCSD | 4105843 | None | `main_frobenius_manifold_mcsd_w384_52f512542f` |
| 4106584 | Frobenius | MCSD-TP | 4105844 | None | `main_frobenius_manifold_mcsd_tp_w384_2f9d20a099` |
| 4106586 | Frobenius | MuonH | 4105840 | None | `main_frobenius_manifold_muonh_w384_58cae627fb` |
| 4106583 | Stiefel | MCSD | 4105837 | 4106582 | `main_stiefel_manifold_mcsd_w384_f4f612e246` |
| 4106585 | Stiefel | MCSD-TP | 4105841 | 4106584 | `main_stiefel_manifold_mcsd_tp_w384_1017e370a6` |
| 4106587 | Stiefel | iMuon | 4105838 | 4106586 | `main_stiefel_manifold_imuon_w384_f3af969879` |

Dependencies use `afterany` to preserve the GPU cap without suppressing a
different geometry's run solely because its predecessor failed. Preserve and
report any failed runs; successful scheduling alone does not establish a pass.

All six saved configurations passed the common-field check. SHA256 of the
canonical shared configuration excluding method-specific momentum:
`a559af424d41934d7e8a9a2650d4c9eaa25a56c7c701854c9f1f1b4897d6d5c2`.
The first three initialized parameter manifests match each other and their
Frobenius pilots byte-for-byte; SHA256
`3ed9fcf1fd63c187d2fa65fbb6e1b0c6c96037e7ec212c21c1c9dfd651f3fa40`.
This verifies metadata/radii/scales, not a hash of the initial weight tensors.

## MuonH is not generally MCSD on a Frobenius sphere

The constraint and return map coincide in this comparison, but the oracle
input does not. Let `P_X` be the Frobenius-orthogonal tangent projector,
`N_R(Y) = R Y / ||Y||_F`, and temporarily give all methods the same momentum
matrix M. The implemented MuonH adapter uses

`N_R(X - eta R msign(M) / ||msign(M)||_F)`.

MCSD uses `Q=P_X M` and `N_R(X - eta c msign(Q))`; MCSD-TP additionally
projects that sign direction before the radial return. In this experiment
`c=R/sqrt(min(m,n))`. For exact full-rank sign, MuonH's normalization gives
that same c, so the main mathematical difference is not a missing constant:
it is the tangent projection **before** the nonlinear matrix sign.

Radial return has first-order action `P_X`, but generally
`P_X msign(M) != P_X msign(P_X M)`. A concrete exact-arithmetic check uses
`X=diag(2,1)`, `R=sqrt(5)`, and `M=diag(3,0.5)`. Then
`Q=diag(0.4,-0.8)`, with `<Q,-msign(M)>=+0.4` and
`<Q,-msign(Q)>=-1.2`. Thus an ambient-Muon direction need not minimize the
tangent linear model, even with the same norm ball and return. This example
illustrates non-equivalence, not a prediction of MuonH's practical loss.

MuonH therefore does not automatically satisfy the paper's small-error LMO
condition for `P_X M`; an arbitrary sufficiently large oracle-error allowance
would not justify the useful small-error bound. Do not describe MuonH as a
covered MCSD special case merely because both normalize back onto a sphere.

There is, however, a conditional inexact-oracle interpretation. For the
same M, exact full-rank sign, matched step scaling and no Nesterov, set
`Q=P_X M`, `S_H=-msign(M)`. Duality gives the directly derived bound
`0 <= zeta_H = <Q,S_H> + ||Q||_* <= 2 ||M-Q||_*`, where `*` is the nuclear
norm. Thus this idealized MuonH can be written as an inexact-MCSD update;
the useful theorem still requires control of this error and all its other
assumptions. With a different oracle input B (such as Nesterov), the analogous
bound is `2 ||B-Q||_*`. The bound does not automatically cover an infeasible
finite-step matrix sign: the actual oracle direction must first satisfy its
norm constraint. These are algebraic observations, not a new convergence
proof for the complete practical MuonH recipe.

For strictly scale-invariant losses without momentum, the current gradient
may already be tangent, making the idealized MCSD and MuonH projection step
coincide under the appropriate full-rank scaling. That special case does not
make accumulated ambient momentum tangent at the current iterate in general.

Sources: the user's `main1.tex`, shared update and Algorithms 1/2; local
`manifold_baselines.py` and `manifold_mcsd.py`; [Hyperball Section 2,
equation (1)](https://arxiv.org/html/2606.16899v1#S2).

Future attribution should match momentum/Nesterov and the actual step norm,
then compare no input TP, input TP only, and input plus output TP. A win by a
baseline at one untuned learning rate is not evidence that the two algorithms
are equivalent, nor proof of which component explains the loss difference.
